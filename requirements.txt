# Alpaca AI Trading Agent - Python Dependencies
# Minimal requirements - mostly stdlib

# Web framework for Application URL
flask>=2.0.0

# WSGI server
gunicorn>=20.1.0

# Environment variable management
python-dotenv>=0.19.0

# No other dependencies - uses stdlib only
# alpaca CLI is external binary, not pip package
# Featherless API uses urllib (stdlib)
# All HTTP requests use urllib (stdlib)
